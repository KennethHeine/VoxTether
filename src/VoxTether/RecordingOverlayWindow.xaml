<Window x:Class="VoxTether.RecordingOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Recording"
        Width="160" Height="40"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual">
    
    <Window.Resources>
        <!-- Pulsing animation for recording indicator -->
        <Storyboard x:Key="PulseAnimation" RepeatBehavior="Forever">
            <DoubleAnimation Storyboard.TargetName="RecordingIndicator"
                             Storyboard.TargetProperty="Opacity"
                             From="1.0" To="0.5" Duration="0:0:0.6"
                             AutoReverse="True"/>
        </Storyboard>
        
        <!-- Spinning animation for loading indicator -->
        <Storyboard x:Key="SpinAnimation" RepeatBehavior="Forever">
            <DoubleAnimation Storyboard.TargetName="LoadingSpinnerRotation"
                             Storyboard.TargetProperty="Angle"
                             From="0" To="360" Duration="0:0:1"/>
        </Storyboard>
    </Window.Resources>
    
    <!-- Main container with dark background -->
    <Border Background="#E6202020" CornerRadius="8" Padding="12,6" 
            HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid>
            <!-- Recording state indicator -->
            <StackPanel x:Name="RecordingPanel" Orientation="Horizontal" Visibility="Visible">
                <!-- Pulsing red circle -->
                <Ellipse x:Name="RecordingIndicator" 
                         Width="14" Height="14" 
                         Fill="#EA4335" 
                         VerticalAlignment="Center"/>
                <TextBlock Text="Recording..." 
                           Foreground="White" 
                           FontSize="13" 
                           FontWeight="Medium"
                           VerticalAlignment="Center"
                           Margin="10,0,0,0"/>
            </StackPanel>
            
            <!-- Transcribing/Loading state indicator -->
            <StackPanel x:Name="TranscribingPanel" Orientation="Horizontal" Visibility="Collapsed">
                <!-- Loading spinner -->
                <Grid Width="14" Height="14" VerticalAlignment="Center">
                    <Ellipse Width="14" Height="14" 
                             Stroke="#4285F4" StrokeThickness="2"
                             Opacity="0.3"/>
                    <Path x:Name="LoadingSpinner"
                          Stroke="#4285F4" StrokeThickness="2"
                          StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                          RenderTransformOrigin="0.5,0.5">
                        <Path.Data>
                            <PathGeometry>
                                <PathFigure StartPoint="7,1" IsClosed="False">
                                    <ArcSegment Point="13,7" Size="6,6" SweepDirection="Clockwise"/>
                                </PathFigure>
                            </PathGeometry>
                        </Path.Data>
                        <Path.RenderTransform>
                            <RotateTransform x:Name="LoadingSpinnerRotation" Angle="0"/>
                        </Path.RenderTransform>
                    </Path>
                </Grid>
                <TextBlock Text="Transcribing..." 
                           Foreground="White" 
                           FontSize="13" 
                           FontWeight="Medium"
                           VerticalAlignment="Center"
                           Margin="10,0,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
